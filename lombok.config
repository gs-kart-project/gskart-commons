# This is the top of the library; nothing above it should influence code generation.
config.stopBubbling = true

# Marks generated accessors, builders and equals/hashCode with @lombok.Generated. JaCoCo skips
# anything carrying a "Generated" annotation, so the coverage numbers reflect hand-written code
# instead of Lombok boilerplate.
lombok.addLombokGeneratedAnnotation = true
